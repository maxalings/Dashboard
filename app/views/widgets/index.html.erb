<h1 class="title">TEST INDEX OF widgets 30px 120x120</h1>
<div class="flex">
    <div class="grid grid-cols-7 gap-5 place-items-start">
        <% 28.times.each_with_index do |_, index| %>
            <div class="bg-zinc-100 widget-layout text-center align-middle"> <%= format('%02d', index + 1) %></div>
            
        <% end %>
    </div>
    <div>

    <div class="text-center">Settings & stuff</div>
    <div class="g-zinc-100 shadow-xl p-30 setting-layout "></div>
    </div>

</div>