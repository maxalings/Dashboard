<% tag ||= :p %>
<%= content_tag(tag, class: "justify-center text-center text-grey-dark", 
                data: { flashcards_handler_target: "text", 
                        question: flashcard.question, 
                        answer: flashcard.answer }) do %>
  <%= flashcard.question %>
<% end %>