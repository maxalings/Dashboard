
<% given_date = Date.new(params[:year].to_i, params[:month].to_i, params[:day].to_i) %>
<% today = Date.today %>
<% days_difference = given_date - today %>

<%= turbo_frame_tag "new_race" do %>
  <%= days_difference.to_i %> DAYS COUNTDOWN!
<% end %>
